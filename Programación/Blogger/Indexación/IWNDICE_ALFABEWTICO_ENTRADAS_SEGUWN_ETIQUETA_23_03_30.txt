<ul id="postList12"></ul>

<script type="text/javascript">

//var url = window.location.href;
//if (url == 'https://jucardus.blogspot.com/' || url == 'https://jucardus.blogspot.com' || url == 'https://jucardus.blogspot.com/?m=1') {

var startIndex = 1;
var maxResults = 150;
var allResults = [];

function sendQuery12()
{
    var scpt = document.createElement("script");
    scpt.src = "https://www.jucardus.com/feeds/posts/summary/-/Mis sitios?alt=json&callback=processPostList12&start-index=" + startIndex + "&max-results=" + maxResults;
    document.body.appendChild(scpt);
}

function printArrayResults(root)
{
  allResults = allResults.sort((a, b) => a.localeCompare(b)); // orden alfabewtico
  allResults = [...new Set(allResults)]; // eliminar elementos repetidos
  allResults = allResults.filter(Boolean); // eliminar elementos vaciwos
  var mostrar = allResults.join("</li>");
  document.getElementById('postList12').innerHTML = mostrar;
}

function processPostList12(root)
{
    var elmt = document.getElementById("postList12");
    if (!elmt)
    return;
    var feed = root.feed;
    if (feed.entry.length > 0)
    {
        for (var i = 0; i < feed.entry.length; i++)
            {
                var entry = feed.entry[i];
                var title = entry.title.$t;
                var date = entry.published.$t;

                for (var j = 0; j < entry.link.length; j++)
                {
                    if (entry.link[j].rel == "alternate")
                    {
                        var url = entry.link[j].href;
                            var url = url.replace('https://jucardus.blogspot.com','');
                        if (url && url.length > 0 && title && title.length > 0)
                        {
                          var fecha = date.substr(2,8).replace(/-/g,'·');
                          var orden = title.toString().toUpperCase().replace(/Á/g,'A').replace(/É/g,'E').replace(/Í/g,'I').replace(/Ó/g,'O').replace(/Ú/g,'U').replace(/Ü/g,'U').replace(/Ñ/g,'N').replace(/[^a-z0-9]/gi, '').replace(/ /g,'');
                          	var entrada = '<li><!--' + orden + '--><a href="' + url + '"><b>' + title.replace(" -","</b> -") + '</a>';
                            allResults.push(entrada);
                        }
                        break;
                    }
                }
            }
        if (feed.entry.length >= maxResults)
        {
            startIndex += maxResults;
            sendQuery12();
        } else {
            printArrayResults();
        }
    }
}
sendQuery12();

//}

</script>

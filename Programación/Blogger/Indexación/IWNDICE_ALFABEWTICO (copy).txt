<!-- CAMBIA «gtranslatec.blogspot.com» POR LA DIRECCIÓN CORRESPONDIENTE -->
<!-- bit.ly/m/agosto -->



<div id="counting" style="padding-bottom: 0px; padding-top: 20px;"></div>
<div id="postList12" style="font-size: 108%; font-weight: bold; margin-left: 20px; padding-bottom: 40px; text-align: left;"></div>

<script type="text/javascript">

var startIndex = 1;
var maxResults = 150;
var allResults = [];
var counter = 0;

function sendQuery12()
{
    var scpt = document.createElement("script");
    scpt.src = "https://gtranslatec.blogspot.com/feeds/posts/summary?alt=json&callback=processPostList12&start-index=" + startIndex + "&max-results=" + maxResults;
    document.body.appendChild(scpt);
}

function printArrayResults(root)
{
    allResults = [...new Set(allResults)]; // eliminar elementos repetidos
    allResults = allResults.filter(Boolean); // eliminar elementos vaciwos
    var mostrar = allResults.join("<p>");
    document.getElementById('postList12').innerHTML = mostrar;
}

function processPostList12(root)
{
    document.getElementById("counting").innerHTML = "&#12288;Collecting...";
    var elmt = document.getElementById("postList12");
    if (!elmt)
    return;
    var feed = root.feed;
    if (feed.entry.length > 0)
    {
        for (var i = 0; i < feed.entry.length; i++)
            {
                var entry = feed.entry[i];
                var title = entry.title.$t;
                var date = entry.published.$t
                for (var j = 0; j < entry.link.length; j++)
                {
                    if (entry.link[j].rel == "alternate")
                    {
                        var url = entry.link[j].href;
                        if (url && url.length > 0 && title && title.length > 0)
                        {
                            var fecha = date.substr(2,8).replace(/-/g,'·');
                                var enlaceFecha = 'https://gtranslatec.blogspot.com/20' + fecha.replace(/·/g,'_') + '_archive.html';
                            var fecha = '<a id="enlaceFecha" target="_blank" href="' + enlaceFecha + '">' + fecha + '</a>';
                            var fecha = '<h3 id="tituloFecha"><mark id="fechasArchivo">' + fecha + '</mark></h3>';
                          	var entrada = '&nbsp;• <a target="_blank" href="' + url + '">' + title + '</a>';
                            allResults.push(fecha);
                            allResults.push(entrada);
                            counter = counter + 1;
                        }
                        break;
                    }
                }
            }
        if (feed.entry.length >= maxResults)
        {
            startIndex += maxResults;
            sendQuery12();
        } else {
            printArrayResults();
            document.getElementById("counting").innerHTML = "&#12288;" + counter + " posts";
        }
    }
}
sendQuery12();
</script>

function addHeader() {

        // Open the active Google Doc

        const doc = DocumentApp.getActiveDocument();

        // Access the header section

        const header = doc.addHeader();

        // Add text or any other elements to the header

        header.appendParagraph("Your Header Text Here").setFontSize(14).setBold(true);
     }

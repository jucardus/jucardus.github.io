function extraer_ultima_linea_sheet() {

  var ss = SpreadsheetApp.openById('1eRy4PPhtghHQX3Sn9sCI0gP5ZNA_JMbzOCRSxo_sAcw');
  var sheet = ss.getSheetByName('Form Responses 1');

  var lastRow = sheet.getLastRow();
  var tema = sheet.getRange(lastRow, 2, 1).getValues();
  var subtema = sheet.getRange(lastRow, 3, 1).getValues();
  var enlace = sheet.getRange(lastRow, 4, 1).getValues();

  Logger.log('\n' + tema + '\n' + subtema + '\n' + enlace);

}
